// The reference to "vite/client" was removed to resolve a "Cannot find type definition file" error.
// This is a workaround for an environment where Vite's type definitions may not be installed.
// The application does not currently use Vite-specific client features, so removing this is safe.
